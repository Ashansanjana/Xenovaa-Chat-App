// Tailwind v4 is applied via the @tailwindcss/vite plugin (see vite.config.js),
// not via PostCSS. This empty config exists only to stop Vite's PostCSS config
// search from picking up an unrelated postcss.config.js higher up the filesystem.
export default {
  plugins: {},
};
